@model course_app.Models.login
@{
             
                ViewBag.Title = "Index";
}

<h1 style="color:indigo">WELCOME :) </h1>
<hr />
<h2>You must login or sign up to continue to website.</h2>



    <div class="panel panel-info">
        <div class="panel-heading">
            <div class="panel-title">Sign In</div>
        </div>

        <div style="padding-top:30px" class="panel-body">

            <div style="display:none" id="login-alert" class="alert alert-danger col-sm-12"></div>
            @using (Html.BeginForm("Login", "Course", FormMethod.Post, new { id = "empform" }))
            {
                <form id="loginform" class="form-horizontal" role="form">

                    <div style="margin-bottom: 25px" class="input-group">
                        <span class="input-group-addon"><i class="glyphicon glyphicon-user"></i></span>
                        @*<input id="login-username" type="text" class="form-control" name="username" value="" placeholder="username or email">*@

                        @Html.TextBoxFor(model => model.email, new { @class = "form-control", @placeholder = "username or email", @type = "email",required="required" })
                    </div>

                    <div style="margin-bottom: 25px" class="input-group">
                        <span class="input-group-addon"><i class="glyphicon glyphicon-lock"></i></span>
                        @*<input id="login-password" type="password" class="form-control" name="password" placeholder="password">*@
                        @Html.TextBoxFor(model => model.password, new { @class = "form-control", @placeholder = "password", @type = "password" ,required="required"})
                    </div>

                    <div class="input-group">

                    </div>


                    <div style="margin-top:10px" class="form-group">
                        <!-- Button -->

                        <div class="col-sm-12 controls">
                            <button id="btn-login" href="#" class="btn btn-success" type="submit">Login  </button>

                        </div>
                    </div>


                    <div class="form-group">
                        <div class="col-md-12 control">
                            <div style="border-top: 1px solid#888; padding-top:15px; font-size:85%">
                                Don't have an account!

                                @Html.ActionLink("Sign Up Here", "Signup", "Course", null)
                            </div>
                        </div>
                    </div>
                </form>


            }
            </div>
        </div>